t_mu = elementof(reals)
t_tau = elementof(posreals)
t_theta = elementof(cartpow(reals, 8))
inputs = (t_mu, t_tau, t_theta)
outputs = logdensityof(posterior, record(mu = t_mu, tau = t_tau, theta = t_theta))
