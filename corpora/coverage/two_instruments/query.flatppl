% Mode ABI: one query, both engines. `mu` is already the model's free `elementof(reals)` boundary, so the query only names the ABI.
lp = logdensityof(posterior, record(mu = mu))
inputs = (mu)
outputs = (lp)
