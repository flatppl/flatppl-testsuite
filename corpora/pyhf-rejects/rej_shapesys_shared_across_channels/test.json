{
  "test_type": "convert",
  "engines": [
    "det-js"
  ],
  "fixture_kind": "pyhf_reject",
  "source": "pyhf.json",
  "oracle": {
    "tool": "pyhf",
    "version": "0.7.6",
    "generator": "corpora/pyhf-rejects/gen_expected.py",
    "quantity": "the converter's exit code and refusal message, beside pyhf's own verdict"
  },
  "pyhf_error": "InvalidModel: Trying to add paramset shapesys/g on b sample in cb channel but other paramsets exist with the same name.",
  "pyhf_agrees": true,
  "checks": [
    {
      "id": "static_integrity",
      "kind": "static_integrity",
      "canonical_sha256": "f2d7733fb0345bb564227eb4f6d9b7879baaf2670b60c39490c83b870fbc72f0"
    },
    {
      "id": "convert_outcome",
      "kind": "convert_outcome",
      "expect_exit": 1,
      "stderr_contains": "unsupported HS3 construct: `shapesys` modifier `g` appears in channels `ca` and `cb`; a `shapesys` parameter is per-channel and cannot be shared, so give each channel's modifier a distinct name"
    }
  ]
}
